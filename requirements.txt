# ==========================================================
# requirements.txt (Production runtime dependencies)
# Primary Care Services - FastAPI backend
# ==========================================================
#
# Why this file is smaller than "pip freeze":
# - Only direct/top-level dependencies are listed.
# - Transitive dependencies (anyio, idna, certifi, etc.) are resolved by pip.
# - This reduces dependency conflicts and makes Azure deployments more reliable.
#
# If you want dev tools (pytest/black/flake8), put them in requirements-dev.txt.
# ==========================================================

# --- Web API framework ---
fastapi==0.110.1
starlette==0.37.2

# --- ASGI server (run locally + Azure startup) ---
uvicorn[standard]==0.25.0
# uvicorn[standard] pulls in: httptools, uvloop (where supported), watchfiles, websockets

# --- MongoDB (Async) ---
motor==3.3.1
pymongo==4.5.0

# --- Config (.env) ---
python-dotenv==1.2.1

# --- Auth / Security ---
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
email-validator==2.3.0

# --- Form uploads (if used anywhere in the API) ---
python-multipart==0.0.21

# --- Outbound email (Resend) ---
resend==2.19.0

# --- HTTP client (often required by SDKs; safe to keep explicit) ---
requests==2.32.5


# ==========================================================
# OPTIONAL: Developer tooling (move to requirements-dev.txt)
# ==========================================================
# black==25.12.0
# flake8==7.3.0
# isort==7.0.0
# mypy==1.19.1
# pytest==9.0.2
# rich==14.2.0
